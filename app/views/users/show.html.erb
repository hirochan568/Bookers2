<h2>User info</h2>
<%= attachment_image_tag @user, :profile_image, :size, 100, 100, fallback: "no_image.jpg" ,format: 'no_image.jpg' %>
<h5>name</h5>
<%= @user.name %>
<h5>introduction</h5>
<%= @user.introduction %>


<h2>UNew book</h2>
<%= form_with(@book,  url: books_path) do |f| %>
<h2>New book</h2>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinipn</h4>
<%= f.text_field :body %>
<%= f.submit 'Create Book' %>
<% end %>


 <h2>Books</h2>
   <table>
   <tr>
     <br>
   <th>Title</th>
   <th>Opinion</th>
   </tr>

   <% @books.each do |book| %>
   <%= link_to user_path(@user.id) do %>
   <tr>
   <td><%= attachment_image_tag book.user, :profile_image, :fill, 20, 20, fallback: "no_image.jpg",size:'20x20',format: 'no_image.jpg' %></td>
   <% end %>
   <td><%=link_to book.title, book_path(book.id)%></td>
   <td><%= book.body %></td>
   </tr>
   <% end %>
   </table>
